'sections' dir:
This is a symlink. The real sections dir is under pollen.docs so there is 
a path dependency.  If pollen.website is cloned into the same dir as 
pollen.docs the relative symlink for 'sections' will work.
